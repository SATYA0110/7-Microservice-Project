apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.catalogService.hpa }}
  namespace: {{ .Values.namespace }}
  labels:
    app: catalog-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.catalogService.deploymentname }}
  minReplicas: {{ .Values.catalogService.minReplicas }}
  maxReplicas: {{ .Values.catalogService.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
